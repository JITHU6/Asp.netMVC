@model IEnumerable<StudentInfo.Models.StudInfo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Department.depname)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Course)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.mark)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Department.depname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Course)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.mark)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StudInfoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.StudInfoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StudInfoId })
        </td>
    </tr>
}

</table>
